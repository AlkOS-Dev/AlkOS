## Purpose
Headers here are part of the Application Programming Interface (ABI). They define the arch - kernel contract 
by providing and exposing definitions for functions and classes that the kernel expects the arch code to *implement*
